# Generated by matugen from {{ image }}

# Wallpaper
output * bg "{{ image }}" fill

# Window shape and spacing
corner_radius 6
default_border pixel 2
gaps outer 2

# Colors
set $bg_color          {{ colors.surface_container.default.hex }}
set $inactive_bg_color {{ colors.surface_container_low.default.hex }}
set $text_color        {{ colors.on_surface.default.hex }}
set $text_dark         {{ colors.on_primary.default.hex }}
set $accent_color      {{ colors.primary.default.hex }}
set $urgent_color      {{ colors.error.default.hex }}

# Window borders
# Class                  Border              Background          Text         Indicator           Child border
client.focused           $accent_color       $accent_color       $text_dark   $accent_color       $accent_color
client.focused_inactive  $bg_color           $bg_color           $text_color  $bg_color           $bg_color
client.unfocused         $inactive_bg_color  $inactive_bg_color  $text_color  $inactive_bg_color  $inactive_bg_color
client.urgent            $urgent_color       $urgent_color       $text_dark   $urgent_color       $urgent_color
