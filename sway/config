# Main Sway config

# Variables
set $mod Mod4
set $left h
set $down j
set $up k
set $right l
set $term foot
set $menu ~/.config/wofi/scripts/launcher.sh

# Local split config
include ~/.config/sway/config.d/*.conf

# System config
include /etc/sway/config.d/*
